//covolution
/*convolution is the method to multiply the power of two signals to make third signal. 
Convolution is used in image processing provides a way of multiplying two images array-matrix-vectors,
Generally different in size put a same dimensionality to produce 3rd vector.*/

clc;
clf;
clear all;
x = input("enter x seq: ");
m = length(x);
subplot(3,1,1);
plot2d3(x);
h = input("enter h seq: ");
n = length(h);
subplot(3,1,2);
plot2d3(h);
z = concol(x,h);
disp(z);
subplot(3,1,3);
plot2d3(z);

//[5,6,7,8]  / [1,1,1,1]